// //arrow function
// cosnt hello = (fruit) => {
//   console.log(fruit);
// }

// hello ("apple");


let className = "bca";
let hello = `my class is ${className}`
console.log(hello);

7let classname = ["bca", "btech", "mca"];
for (let i = 0; i < classname.length; i++) {
  let hello = `my class is ${classname[i]}`;
  console.log(hello);
}